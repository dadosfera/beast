NbSearch: a search icon trigger that opens a full-screen overlay with a heading-1 sized input.

```jsx
<Search onSearch={(q) => …} placeholder="Buscar datasets" />
```

- Esc closes; Enter calls `onSearch`.
