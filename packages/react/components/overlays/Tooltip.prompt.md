nbTooltip: dark caption bubble (.5rem 1rem) with an arrow, shown on hover or click.

```jsx
<Tooltip text="Excluir" position="bottom"><Button appearance="ghost" icon="trash-2-outline" /></Tooltip>
```

- `status` variants fill with the status color (primary uses primary-900).
