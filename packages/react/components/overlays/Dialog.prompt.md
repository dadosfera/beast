NbDialogService: a Card centered over a dimmed backdrop with title, body and footer actions.

```jsx
<Dialog open={open} onClose={…} title="Excluir dataset?" footer={<><Button appearance="ghost" onClick={…}>Cancelar</Button><Button status="danger">Excluir</Button></>}>Esta ação não pode ser desfeita.</Dialog>
```


