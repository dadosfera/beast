nbPopover: white shadowed panel with an arrow anchored to its trigger.

```jsx
<Popover content={<div>Conteúdo</div>} position="bottom"><Button>Abrir</Button></Popover>
```

- `trigger`: click | hover | hint | focus | noop (controlled via `open`).
