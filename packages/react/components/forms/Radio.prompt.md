NbRadio / NbRadioGroup: 1.25rem circles with a primary inner dot; RadioGroup manages a single selection.

```jsx
<RadioGroup options={['Diário','Semanal']} defaultValue="Diário" />
```

- `direction` row | column (default column).
