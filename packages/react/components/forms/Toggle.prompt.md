NbToggle: 3.125rem pill switch with a white 1.75rem knob that shows a checkmark when on.

```jsx
<Toggle label="Notificações" defaultChecked />
```

- `labelPosition` left | right. `status` recolors the on state.
