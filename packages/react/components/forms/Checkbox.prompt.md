NbCheckbox: 1.25rem box with 3px radius that fills primary when checked; supports indeterminate.

```jsx
<Checkbox label="Aceito os termos" defaultChecked />
<Checkbox indeterminate label="Selecionar tudo" />
```

- `status` changes the checked fill color.
