NbSidebar: 16rem white side panel (3.5rem when compacted) with optional header/footer slots.

```jsx
<Sidebar header={<img src="…" />}><Menu items={items} /></Sidebar>
```

- `state`: expanded | compacted | collapsed. `right` docks it on the right.
