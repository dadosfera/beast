NbStepper: numbered 2rem circles joined by connectors; completed steps fill primary, the active one has a primary border.

```jsx
<Stepper steps={[{label:'Fonte',content:…},{label:'Mapeamento'},{label:'Revisão'}]} />
```

- `orientation` horizontal | vertical. `linear` blocks jumping ahead. `showControls` renders Anterior/Próximo.
