NbList / NbListItem: divided vertical list (1rem item padding); `onScrollEnd` supports infinite loading.

```jsx
<List items={rows} renderItem={(r) => <span>{r.name}</span>} style={{maxHeight:'20rem'}} />
```


