NbAccordion: stacked expandable items with subtitle headers and a chevron, grouped in one shadowed panel.

```jsx
<Accordion items={[{title:'Fontes',content:'…',expanded:true},{title:'Destinos',content:'…'}]} />
```

- `multi` allows several open at once.
