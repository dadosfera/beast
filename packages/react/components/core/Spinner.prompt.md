NbSpinner: rotating ring, inline with an optional message or as an overlay covering its relative parent.

```jsx
<Spinner status="primary" message="Carregando…" />
<div style={{position:'relative'}}>…<Spinner overlay status="primary" /></div>
```

- sizes tiny…giant.
