NbButtonGroup with nbButtonToggle: adjacent buttons sharing one rounded frame, single or multiple selection.

```jsx
<ButtonGroup options={['Dia','Semana','Mês']} defaultValue="Semana" status="primary" />
```

- `multiple` for toggle-many; `onChange` receives the value (or array).
- Same appearance/status/size/shape as Button.
